# Step 5: DSP 基礎（FFT・フィルター）

## 学習目標

Accelerate フレームワークの `vDSP` を使い、FFT によるスペクトル解析とフィルター設計を実装する。
「カラオケのピッチ検出はどう動いているか」を FFT の観点から説明できるようになる。

---

## 学べること

### 1. FFT（Fast Fourier Transform）

#### 時間領域 → 周波数領域への変換

音声データは本来「時間とサンプル値」の関係（時間領域）で表される。
FFT はこれを「周波数とマグニチュード」の関係（周波数領域）に変換する。

```
時間領域:                    周波数領域（FFT後）:
振幅                         マグニチュード
│ ∿∿∿∿∿∿∿∿                  │     ↑
│                      →     │     │
└──────── 時間               └──┬──┬──┬── 周波数
                                440 880 1320 Hz
```

サイン波が FFT されると**その周波数の位置だけにスパイクが立つ**。
複数の周波数が混ざった和音は、それぞれの周波数にスパイクが立つ。

#### vDSP による実装（5ステップ）

```swift
// Step 1: Hanning ウィンドウを乗算（スペクトルリーク低減）
vDSP_vmul(samples, 1, window, 1, &windowed, 1, vDSP_Length(fftSize))

// Step 2: 実数配列 → DSPSplitComplex（実部・虚部を分離）
vDSP_ctoz(complexPtr, 2, &split, 1, vDSP_Length(binCount))

// Step 3: 高速フーリエ変換
vDSP_fft_zrip(fftSetup, &split, 1, log2n, FFTDirection(FFT_FORWARD))

// Step 4: 複素数 → マグニチュード（|z| = √(r² + i²)）
vDSP_zvabs(&split, 1, &magnitudes, 1, vDSP_Length(binCount))

// Step 5: dB 変換（20 * log10）
vDSP_vdbcon(magnitudes, 1, &ref, &result, 1, vDSP_Length(binCount), 0)
```

#### FFT サイズと周波数分解能

```
周波数分解能（Hz/bin） = サンプルレート / FFT サイズ

例: サンプルレート 48000 Hz、FFT サイズ 2048 の場合
  → 48000 / 2048 ≒ 23.4 Hz/bin

440 Hz は bin 番号 440 / 23.4 ≒ 18.8 → bin 19 付近にスパイク
```

FFT サイズを大きくすると周波数分解能が上がるが、必要なサンプル数が増えるため時間分解能が下がる。

#### 時間分解能とは

FFT は「N 個のサンプルが揃ってから初めて計算できる」処理。サイズが大きいほど 1 フレームの計算に時間がかかる。

```
FFT サイズ 2048、サンプルレート 48000 Hz:
  2048 / 48000 = 42.7 ms → 42.7 ms ごとにしかスペクトルが得られない

FFT サイズ 512、サンプルレート 48000 Hz:
  512 / 48000 = 10.7 ms → 10.7 ms ごとに更新（4倍の頻度）
```

| FFT サイズ | 周波数分解能 | 1フレームの時間 | 時間分解能 |
|---|---|---|---|
| 512 | 93.8 Hz/bin | 10.7 ms | 高い（素早く更新）|
| 2048 | 23.4 Hz/bin | 42.7 ms | 低い（更新が遅い）|
| 8192 | 5.9 Hz/bin | 170.7 ms | 非常に低い |

これは物理の**ハイゼンベルクの不確定性原理**と同じ構造。

```
周波数分解能 × 時間分解能 = 定数（どちらかを上げるともう一方が下がる）
```

**カラオケへの影響**
- FFT サイズ 小: 素早く反応するが、440 Hz と 493 Hz（ラ♭）の区別が難しい
- FFT サイズ 大: 精度が高いが更新が遅くなりリアルタイム追従が遅れる

#### installTap のバッファサイズと FFT サイズの関係

```swift
engine.mainMixerNode.installTap(onBus: 0, bufferSize: 2048, format: tapFormat) { buffer, _ in
    // buffer.frameLength は必ずしも 2048 にはならない（あくまで要求値）
}
```

`bufferSize` は**要求値**であり、AVAudioEngine が実際に届けるサイズは異なる場合がある。

| ケース | 挙動 |
|---|---|
| tap バッファ = FFT サイズ | 理想的。1コールバックで FFT 1回分のデータが揃う |
| tap バッファ < FFT サイズ | サンプルが不足。現実装では FFT の精度が下がる |
| tap バッファ > FFT サイズ | 余分なサンプルを切り捨てる（現実装）|

**プロダクション環境での正しいアプローチ**

サンプルを**リングバッファに蓄積**し、FFT サイズ分揃ったタイミングで実行する。
さらに **50% オーバーラップ**（前回の後半 + 今回の前半）で FFT することで時間分解能も改善できる。

```
オーバーラップなし:  [──A──][──B──][──C──]  ← 42.7 ms ごと
50% オーバーラップ: [──A──]
                       [──B──]               ← 21.3 ms ごとに更新
                          [──C──]
```

本実装は教育用の簡略版のため `min(frameLength, fftSize)` で処理しているが、
実際のスペクトラムアナライザーや音程検出では蓄積バッファ + オーバーラップが標準的。

#### Hanning ウィンドウとは

FFT は入力が「周期的に繰り返す」と仮定する。バッファの端でサンプル値が不連続だと**スペクトルリーク**（本来ないはずの周波数成分が現れる）が起きる。

```
ウィンドウなし:  |急激な不連続| → スペクトルが滲む
Hanning ウィンドウ: 両端をゼロに向かってなめらかにフェード → リークを抑制
```

```swift
vDSP_hann_window(&window, vDSP_Length(fftSize), Int32(vDSP_HANN_NORM))
vDSP_vmul(samples, 1, window, 1, &windowed, 1, vDSP_Length(fftSize))
```

#### カラオケへの応用（ピッチ検出）

```
マイク入力 → FFT → 最大マグニチュードの bin を探す
                        ↓
              bin番号 × (サンプルレート / FFT サイズ) = 基本周波数
                        ↓
              基準音（楽譜の音符）との差 = ピッチのズレ量
```

---

### 2. 一次 IIR ローパスフィルター

#### 仕組み

```
y[n] = α × x[n] + (1 - α) × y[n-1]

α = 2π × fc / (2π × fc + fs)
  fc: カットオフ周波数
  fs: サンプルレート
```

前の出力サンプル `y[n-1]` を `(1-α)` の割合で混ぜ続けることで、
急激な変化（高周波成分）が緩やかに平滑化される。

#### ロールオフ特性

```
0 dB ──────────────┐
                   │ ← -3 dB（カットオフ点 fc）
                   fc
                      ＼
                        ＼ -20 dB/decade（10倍の周波数ごとに 20 dB 減衰）
                          ＼
                           10×fc
```

カットオフ周波数で -3 dB。そこから 1 decade（10倍）ごとに 20 dB ずつ減衰する。

#### FFT での確認

| 周波数 | カットオフとの関係 | 減衰量（目安）|
|---|---|---|
| 440 Hz | カットオフ（800 Hz）以下 | ほぼ 0 dB |
| 880 Hz | カットオフより上 | -5 dB 程度 |
| 1320 Hz | カットオフより上 | -8 dB 程度 |

---

### 3. Biquad ローパスフィルター（二次バターワース）

#### 仕組み

二次の差分方程式（過去 2 サンプルを参照）：

```
y[n] = b0×x[n] + b1×x[n-1] + b2×x[n-2] - a1×y[n-1] - a2×y[n-2]
```

伝達関数：

```
H(z) = (b0 + b1×z⁻¹ + b2×z⁻²) / (1 + a1×z⁻¹ + a2×z⁻²)
```

#### バターワース係数（Q = 1/√2）

```swift
let omega    = 2π × fc / fs
let alpha    = sin(omega) / (2 × Q)  // Q = 1/√2

let b0 = (1 - cos(omega)) / 2
let b1 =  1 - cos(omega)
let b2 = (1 - cos(omega)) / 2
let a0 =  1 + alpha           // 正規化用
let a1 = -2 × cos(omega)
let a2 =  1 - alpha
```

Q = 1/√2 がバターワース特性。通過域が最もフラット（リップルなし）になる。

#### vDSP_biquad による実装

```swift
// 係数を渡してセットアップを生成（Double で指定、Float 処理に使用）
let setup = vDSP_biquad_CreateSetup(coefficients, 1) // 1 section = 二次

// delay: フィルターの内部状態（サイズ = 2 × (sections + 1)）
var delay = [Float](repeating: 0, count: 4)

// フィルター適用（in ≠ out の別バッファ）
vDSP_biquad(setup, &delay, input, 1, &output, 1, vDSP_Length(count))
```

#### ロールオフ特性

```
0 dB ──────────────┐
                   fc ← -3 dB
                      ＼
                        ＼ -40 dB/decade（一次IIRの2倍の急峻さ）
                          ＼＼
                             10×fc
```

---

### 4. フィルター比較

| | 一次 IIR | Biquad（二次バターワース）|
|---|---|---|
| ロールオフ | -20 dB/decade | -40 dB/decade |
| 次数 | 1次 | 2次 |
| 係数 | α（1個）| b0, b1, b2, a1, a2（5個）|
| 実装 | 手動ループ | `vDSP_biquad` |
| 用途 | 簡易スムージング | EQ・音声フィルター全般 |
| カスケード | N段で N次 | N段で 2N次 |

カスケード（直列接続）するほど急峻なカットオフになる。
プロ音楽アプリの EQ は Biquad を複数段カスケードして実現している。

---

## 確認手順（アプリでの操作）

### 確認1: 単音と和音の FFT 比較

1. 「440 Hz（単音）」を選択 → 440 Hz 付近だけスパイク
2. 「440 + 880 + 1320 Hz（和音）」を選択 → 3箇所にスパイク

**なぜ重要か**  
FFT が「音の成分分解」であることを視覚的に確認できる。880 Hz は 440 Hz の倍音（オクターブ上）であり、楽器の音色がこの倍音構造で決まる。

---

### 確認2: 一次IIR と Biquad の比較

1. 「和音 + 一次IIR LPF」→ 880 Hz・1320 Hz が緩やかに低下
2. 「和音 + Biquad LPF」→ 880 Hz・1320 Hz がより急峻に低下

**確認すること**
- 同じ 800 Hz カットオフでも Biquad の方がスパイクが明確に落ちている
- 一次IIRは 800 Hz より上でも完全には消えていない

---

## 次のステップへの問い

1. FFT のサイズを 2048 から 512 に変えると周波数分解能はどう変わるか？ なぜそのトレードオフが存在するか？
2. `vDSP_biquad` の `delay` バッファが `Float`（`vDSP_biquadD` なら `Double`）である理由は？
3. Biquad を 2 段カスケードすると何次フィルターになるか？ ロールオフは何 dB/decade になるか？
